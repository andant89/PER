# Copiez ce fichier en « .env » à côté du docker-compose, puis adaptez la valeur.
# C'est le port publié sur la machine hôte (le NAS), celui que vise le reverse
# proxy. Le port interne du conteneur reste 80 dans tous les cas.
PORT_NAS=8087
